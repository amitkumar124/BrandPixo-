
export interface Preset {
  name: string;
  prompt: string;
}

export interface AspectRatio {
  label: string;
  value: string;
}

export interface StockModel {
  id: number;
  url: string;
  alt: string;
}
